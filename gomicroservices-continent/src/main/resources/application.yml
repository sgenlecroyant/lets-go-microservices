# application level config
spring:
  application:
    name: continent-service

# cloud config 
  cloud:
    config:
      enabled: false
    
# server config
server:
  port: 8001
  
# Eureka Client config
eureka:
  client:
    enabled: false
